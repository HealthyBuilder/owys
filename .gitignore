node_modules/
target/
.anchor/

# Local runtime state — none of this belongs in the repo.
# NOTE: gitignore only honours '#' at the start of a line; a trailing comment
# becomes part of the pattern and silently fails to match.

# contains cardholder PRIVATE KEYS
.demo-users.json

# NOTE: .mock-tickers.json is intentionally NOT ignored. It holds only devnet
# mint addresses (no secrets) and both a fresh clone and the deployed image
# need it to start.
ledger.json
.program-id
*.db
*.db-journal
.env
.DS_Store
